# 專案維護規準

本專案是一份同時供線上閱讀與 A4 列印／另存 PDF 使用的講義。修改內容、元件或樣式時，必須保留以下版面規則；除非使用者明確要求，不得為了「自動排版」而重設既有 Grid 或人工換頁點。

網站的頁面順序、檔案用途與後續分檔規劃記錄在 `SITE_STRUCTURE_INDEX.md`。開始編修前先閱讀該索引；頁面新增、刪除、改名、換頁或搬移內容後，必須同步更新索引。

## 編修範圍與檔案隔離

- 目前架構是「每張 A4 一個 React／TSX 頁面元件」，不是彼此無關的純 HTML 檔；入口頁只負責依序組合所有頁面。
- 每張頁面必須有穩定且唯一的頁面識別，例如 `data-page="p05"`。頁碼改變時，先確認內容身分，再更新索引與測試，不可只依賴舊有 `.page5`、`.page11` 等 class 名稱判斷頁次。
- 單頁內容修改預設只可變更 `app/handout/pages/PageNN.tsx` 與 `app/handout/styles/PageNN.module.css`。若需要修改共用元件、共用 CSS、列印 CSS 或入口頁，必須先檢查所有頁面的影響。
- 單頁排版使用該頁的 CSS Module 或以穩定頁面識別為範圍的局部規則；禁止為了修一頁而新增無範圍限制的全域選擇器。
- 臨時微調 class 或診斷標記只能存在於正在處理的單頁範圍，驗收完成後必須移除。不可將紅框、溢位提示或診斷文字發布到正式網站。
- 共用色彩、字級、A4 幾何、表格、提示框、卡片與列印規則集中管理；頁面專屬檔案不可複製並改寫整套共用規則。
- 保留整份講義的向下捲動閱讀與一次列印能力。若日後加入上一頁／下一頁閱讀器，應共用同一批頁面元件，不得維護第二份講義內容。

## 變更流程

1. 先在 `SITE_STRUCTURE_INDEX.md` 找到目標頁及其保護規則。
2. 只修改目標頁檔案與頁面專屬樣式；若擴大到共用檔案，記錄原因與可能受影響頁面。
3. 檢查目標頁內容順序、字級、Grid、A4 高度及頁尾空白。
4. 執行建置與自動測試；頁數測試必須與索引中的實際頁數一致。
5. 檢查整份講義沒有新增溢位、頁次錯位或共用樣式回歸，再考慮部署。

## 專案檔案保護

- 必須保留 `.openai/hosting.json` 的既有 `project_id`，不得建立或替換成另一個 Sites 專案。
- `dist/` 與 `.vinext/` 是可重新產生的建置輸出或快取，不是內容原稿，不得直接編修。
- `worker/`、`build/`、`scripts/`、Vite／Vinext／Next 設定及 lockfile 屬於部署骨架；除非建置需求明確，不得為了整理目錄而改寫。
- `db/`、`drizzle/`、`examples/d1/`、`drizzle.config.ts` 與 `app/chatgpt-auth.ts` 是目前未啟用的選配功能。移除前必須同時檢查套件、建置設定與 lockfile，且完整建置通過。
- `public/assets/` 中由講義引用的圖片不得因清理預設素材而刪除。

## 字級與基本排版

- 主要內文固定為 12pt。
- 標籤、表格、圖說、提示等輔助文字不得小於 10pt。
- 封面與每一張內容頁都必須是獨立 `.sheet`；每個 `.sheet` 固定為 210mm × 297mm。
- `@page` margin 固定為 0；頁面安全邊界由 `.sheet` padding 管理，網站畫面與列印使用同一組 A4 幾何。
- 不得恢復「連續長頁＋列印時自動分頁」架構。換頁必須直接對應 React 中相鄰的 `<Page>` 元件。
- 每頁使用 `height: 297mm`、`overflow: hidden`；任何 `scrollHeight > clientHeight` 都是必須修正的溢位錯誤，不可視為可接受的自然跨頁。
- 標題不得孤立在頁尾；標題後至少要能容納一段正文或一個短元件，否則標題與內容一起移至下頁。
- 不可依賴全域 `break-inside` 或瀏覽器臨時分頁修補。內容要跨頁時，必須在 JSX 中拆成下一個 `<Page>`，並維持內容順序。

## Grid 選擇原則

- Grid 欄數由內容長度與比較關係決定，不得因後續修改而一律改成單欄。
- 四張長度相近、彼此並列的卡片，預設使用 **2×2 Grid**。
- 兩個需要直接比較的內容，使用 **雙欄 Grid**。
- 五張短卡片可使用 **第一列 3 張、第二列 2 張**；必要時讓第二列兩張等寬展開。若文字變長，改成 2 欄分列，不可硬壓成過高或過窄的卡片。
- 三至四個非常短的標籤或修訂詞，可分別使用 3 欄或 4 欄；如果每格已形成段落，就降低欄數。
- 卡片內若只有短標題與一兩行文字，移除不必要的固定高度與大面積 padding，讓卡片高度由內容決定。
- 同一組卡片要維持一致的視覺寬度與間距，但不以強制等高製造大片空白。
- 表格、Grid 與左右對照的欄寬要依內容分配；步驟編號、標籤等短欄應較窄，說明文字欄應取得主要寬度。

## Grid 的列印規則

- A4 列印寬度可能命中螢幕版的窄版 media query，因此 `@media print` 必須明確恢復 `.twoCols`、`.caseGrid`、`.formGrid`、`.goalGrid`、`.slideMap` 等為 2 欄，`.threeCols`／`.fiveQuestions` 為 3 欄，`.revisionGrid` 為 4 欄。
- 行動版可以塌成單欄，但列印版不得沿用行動版單欄規則。
- 單張卡片不可被跨頁切開。
- Grid 不得跨越 `.sheet`。若整組放不下，先縮減多餘 padding、margin 與固定高度；仍放不下時，才在 JSX 中以完整列為單位拆到下一頁。
- 2×2 Grid 拆頁時以完整一列為單位；不可讓同一列的兩張卡片分到不同頁。
- Grid 前的標題、導言與第一列應視為一個閱讀單位；頁尾放不下第一列時，整組從下一頁開始，不可只留下表頭。

## 人工換頁與內容密度

- 不再使用 `.printBreak`。封面與目前掛載的14張內容頁都由獨立 Page 元件鎖定，不可合併回連續文流。
- Step 2 固定只占兩頁：P5 依序包含案例、案例提示語、「把案例提示語換成你的講義需求」標題與講義目錄表；P6 依序包含講義通用提示語、樣張批判修訂、學習單表格與其後全部操作產出。不得再拆回三頁，也不得在合併處補上重複標題。
- P5、P6 的兩張表格以縮減列高、儲存格 padding 與區塊間距控制容量，表格文字仍不得小於 10pt；不可為了塞頁刪改內容。
- Step 4 的內容順序固定為「P9 情境導入 → P10 互動示意與規格 → P11 Netlify 登入 → P12 首次部署與後續維護」；P11–P12 固定整併為兩頁，不得刪改原有操作內容。
- 調整頁面容量時，依序縮減：多餘卡片最小高度、卡片 padding、元件間 margin；不得先縮小字體，也不得破壞 Grid 欄數。
- 若頁尾只剩兩三行空間，可讓下一個完整段落或元件換頁；但不可留下約 1/3～1/2 頁的空白。
- 長提示詞若需要跨頁，必須在語意完整處拆成兩個明確頁面元件，不可讓列印引擎任意截斷邊框或文字。

## 修改後必做驗收

- 同時檢查桌面閱讀、手機閱讀與 A4 輸出；桌面版每張 `.sheet` 的可見邊界必須與列印頁面一致。
- 每頁會在內部檢查 `scrollHeight > clientHeight`，但不得在講義畫面或列印結果中顯示溢位標籤、紅框或其他檢查介面；任何偵測到的溢位都不得發布。
- 逐頁檢查：人工換頁點、Step 起始頁、孤立標題、Grid 欄數、同列卡片是否拆散、卡片是否被切開、是否出現大片空白。
- 特別檢查 P3 的 2 欄填寫 Grid，以及 P10 的滿寬導言與五張互動規格卡片。
- 若無法實際取得列印預覽，不得宣稱分頁已驗證；只能說明已完成規則或程式層面的檢查。
